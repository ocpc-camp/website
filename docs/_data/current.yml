# Pointer to the current live season. The root docs/index.html redirects here,
# and that season's own folder (docs/<season>/) IS the live portal — a
# self-contained, per-season artifact (so archiving is just leaving it in place
# and pointing this at the next season).
#
# To go live with a new season: copy the previous season folder to docs/<new>/,
# edit it, then change this pointer.
current_season: 2026s
